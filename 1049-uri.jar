import java.util.Scanner;

public class Main {

    
    public static void main(String args[]) {
       Scanner sc = new Scanner(System.in);
       String tipo1 = sc.next(); 
       String tipo2 = sc.next();
       String tipo3 = sc.next();
       
       if ("vertebrado".equals(tipo1) && "ave".equals(tipo2) && "carnivoro".equals(tipo3)){
           System.out.printf("aguia\n");
       }
       if ("vertebrado".equals(tipo1) && "ave".equals(tipo2) && "onivoro".equals(tipo3)){
           System.out.printf("pomba\n");
       }
       if ("vertebrado".equals(tipo1) && "mamifero".equals(tipo2) && "onivoro".equals(tipo3)){
           System.out.printf("homem\n");
       }
       if ("vertebrado".equals(tipo1) && "mamifero".equals(tipo2) && "herbivoro".equals(tipo3)){
           System.out.printf("vaca\n");
       }
       if ("invertebrado".equals(tipo1) && "inseto".equals(tipo2) && "hematofago".equals(tipo3)){
           System.out.printf("pulga\n");
       }
       if ("invertebrado".equals(tipo1) && "inseto".equals(tipo2) && "herbivoro".equals(tipo3)){
           System.out.printf("lagarta\n");
       }
       if ("invertebrado".equals(tipo1) && "anelideo".equals(tipo2) && "hematofago".equals(tipo3)){
           System.out.printf("sanguessuga\n");
       }
       if ("invertebrado".equals(tipo1) && "anelideo".equals(tipo2) && "onivoro".equals(tipo3)){
           System.out.printf("minhoca\n");
       }
    }
    
}